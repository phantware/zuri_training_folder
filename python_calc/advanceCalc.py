# Advance Calculator
class Calculator:

 # This is a stact-based calculator
 # Uses parenthesis for order of Operation
 # Implemets addition, subtraction, division and multiplication

   def calculate(self, s):
    # Takes in a string s and returns a number
      return
print("Launching the calculator...")
calc = Calculator()
user_input = ""
while user_input not in ["exit","quite","stop", "close"]:
 user_input = input("> ")
 output = calc.calculate(user_input)